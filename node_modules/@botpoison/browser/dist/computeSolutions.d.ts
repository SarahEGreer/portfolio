import { ProgressCallback, SignedChallenge } from "./index";
declare const computeSolutions: ({ challenge, onProgress, }: {
    challenge: SignedChallenge;
    onProgress?: ProgressCallback | undefined;
}) => Promise<string[]>;
export default computeSolutions;
